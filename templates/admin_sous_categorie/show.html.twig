{% extends 'base-backend.html.twig' %}
{% block title %}SousCategorie{% endblock %}

{% block body %}
    <h1>SousCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sous_categorie.nom }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ sous_categorie.slug }}</td>
            </tr>
            <tr>
                <th>Image_name</th>
                <td>{{ sous_categorie.imageName }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ sous_categorie.updatedAt ? sous_categorie.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_sous_categorie_index') }}">back to list</a>

    <a href="{{ path('app_admin_sous_categorie_edit', {'id': sous_categorie.id}) }}"class= "btn btn-dark">Modifier</a>

    {{ include('admin_sous_categorie/_delete_form.html.twig') }}
{% endblock %}
