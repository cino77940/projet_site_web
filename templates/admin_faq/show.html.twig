{% extends 'base-backend.html.twig' %}
{% block title %}Faq{% endblock %}

{% block body %}
    <h1>Faq</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ faq.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ faq.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ faq.texte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_faq_index') }}">Retour à la liste</a>

    <a href="{{ path('app_admin_faq_edit', {'id': faq.id}) }}">Modifier</a>

    {{ include('admin_faq/_delete_form.html.twig') }}
{% endblock %}
