{% extends 'base-backend.html.twig' %}

{% block title %}Carousel{% endblock %}

{% block body %}
    <h1>Carousel</h1>
<div class="container-fluid my-3">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carousel.id }}</td>
            </tr>
            <tr>
                <th>Image_name</th>
                <td>{{ carousel.imageName }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ carousel.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ carousel.texte }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ carousel.updatedAt ? carousel.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_carousel_index') }}" class="btn btn-dark">Retour à la liste</a>

    <a href="{{ path('app_admin_carousel_edit', {'id': carousel.id}) }}"class="btn btn-dark">Modifier</a>

    {{ include('admin_carousel/_delete_form.html.twig') }}
</div>
{% endblock %}
